apiVersion: v1
kind: Service
metadata:
  name: {{ include "ilanas-website.fullname" . }}
  labels:
    {{- include "ilanas-website.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ilanas-website.selectorLabels" . | nindent 4 }}
